groups:
  - name: application-alerts
    rules:
      - alert: DjangoEndpointDown
        expr: up{job="django"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Django endpoint is down"
          description: "No targets are reachable for the Django service for 1 minute."